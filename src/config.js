import { Platform } from 'react-native';

// Use standard local IP or 10.0.2.2 for Android Emulator, localhost for iOS simulator
// Make sure to replace this with your actual local IP address when testing on a physical device.
export const BASE_URL = 'http://10.247.225.56:5000/ap';
